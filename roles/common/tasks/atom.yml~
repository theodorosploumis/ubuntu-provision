---
- name: install Atom editor - add atom ppa
  apt_repository:
    repo: 'ppa:webupd8team/atom'
    state: present
    update_cache: yes
  sudo: yes

- name: install Atom editor
  apt:
    name: atom
    update_cache: yes
  sudo: yes
