---
- name: Clone my dotfiles
  git:
    repo: "git://github.com/theodorosploumis.com/dotfiles.git"
    dest: "/home/{{ username }}/"
    force: no
    accept_hostkey: yes
  when: result.rc !=0
  tags: dotfiles
